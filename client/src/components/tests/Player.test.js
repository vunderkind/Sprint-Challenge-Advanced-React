import * as PlayerData from '../PlayerData';


describe('PlayerData', () => {
  it('makes a person with name and age', () => {
    expect(PlayerData(props.name))
      .toMatchObject({
      });
  });

  // write more tests! <===========================================
});